import 'package:flutter/material.dart';

/// Islamic green color palette for Muallimi Soniy.
/// Green was the beloved color of Prophet Muhammad ﷺ.
class AppColors {
  AppColors._();

  // Primary brand — Islamic Green
  static const Color primary = Color(0xFF2E7D32);
  static const Color primaryLight = Color(0xFF66BB6A);
  static const Color primaryDark = Color(0xFF1B5E20);
  
  // Secondary / Accent — Green variants
  static const Color secondary = Color(0xFF43A047);
  static const Color accent = Color(0xFF81C784);
  
  // Gold for premium feel (kept — gold is also used in Islamic art)
  static const Color gold = Color(0xFFD4A852);
  static const Color goldLight = Color(0xFFE8C976);

  // Semantic
  static const Color success = Color(0xFF00B894);
  static const Color warning = Color(0xFFF9CA24);
  static const Color error = Color(0xFFE17055);
  static const Color info = Color(0xFFA5D6A7);

  // Light theme — green-tinted
  static const Color backgroundLight = Color(0xFFF1F8E9);
  static const Color surfaceLight = Color(0xFFE8F5E9);
  static const Color cardLight = Colors.white;
  static const Color borderLight = Color(0xFFC8E6C9);
  static const Color textDark = Color(0xFF1A2E1A);
  static const Color textSecondary = Color(0xFF5C6B5C);
  
  // Dark theme — deep green
  static const Color backgroundDark = Color(0xFF0A1F0A);
  static const Color surfaceDark = Color(0xFF132613);
  static const Color cardDark = Color(0xFF1B2E1B);
  static const Color borderDark = Color(0xFF2D4A2D);
  static const Color textLight = Color(0xFFE8EAED);
  
  // Shared
  static const Color textMuted = Color(0xFF7A8A7A);
  
  // Arabic text
  static const Color arabicGold = Color(0xFFD4A852);
  static const Color arabicHighlight = Color(0x332E7D32);
}
